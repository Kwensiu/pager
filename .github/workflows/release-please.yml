name: Release Please

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write
  issues: write

jobs:
  release-please:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Verify release token is configured
        env:
          RELEASE_PLEASE_ACTION_TOKEN: ${{ secrets.RELEASE_PLEASE_ACTION_TOKEN }}
        run: |
          if [ -z "$RELEASE_PLEASE_ACTION_TOKEN" ]; then
            echo "RELEASE_PLEASE_ACTION_TOKEN is required so release events can trigger downstream workflows."
            exit 1
          fi

      - name: Run release-please
        id: release
        uses: googleapis/release-please-action@v4
        with:
          token: ${{ secrets.RELEASE_PLEASE_ACTION_TOKEN }}
          config-file: .github/.release-please-config.json
          manifest-file: .github/.release-please-manifest.json
